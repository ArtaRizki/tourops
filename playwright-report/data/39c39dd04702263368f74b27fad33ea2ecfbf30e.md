# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: supplier.spec.ts >> Supplier Role Tests (CRUD) >> Rate Cards: Create, Update, Delete
- Location: tests\supplier.spec.ts:22:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=E2E Grand Hotel 1780440806038').first()
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for locator('text=E2E Grand Hotel 1780440806038').first()

```

```yaml
- dialog "Create New Rate":
  - heading "Create New Rate" [level=2]
  - text: Hotel Name
  - textbox: E2E Grand Hotel 1780440806038
  - text: Room Type
  - textbox: Deluxe Suite
  - text: Price Per Night
  - spinbutton: "250"
  - text: Valid From
  - textbox: 2026-06-03
  - text: Valid To
  - textbox
  - button "Cancel"
  - button "Save Rate"
  - button "Close":
    - img
    - text: Close
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Supplier Role Tests (CRUD)', () => {
  4  |   const rateData = {
  5  |     hotelName: 'E2E Grand Hotel ' + Date.now(),
  6  |     roomType: 'Deluxe Suite',
  7  |     price: '250',
  8  |     newPrice: '275'
  9  |   };
  10 | 
  11 |   test('Login and navigate supplier dashboard', async ({ page }) => {
  12 |     await page.goto('/staff/login');
  13 |     await page.fill('#username', 'hotelmanager1');
  14 |     await page.fill('#password', 'password123');
  15 |     await page.click('button[type="submit"]');
  16 | 
  17 |     await page.waitForURL('/supplier');
  18 |     await expect(page.locator('h1').first()).toContainText('Hotel Dashboard'); // Based on hotelmanager1 role (hotel_manager)
  19 |     await expect(page.getByTestId('text-supplier-title')).toBeVisible();
  20 |   });
  21 | 
  22 |   test('Rate Cards: Create, Update, Delete', async ({ page }) => {
  23 |     await page.goto('/staff/login');
  24 |     await page.fill('#username', 'hotelmanager1'); // hotelmanager1 is a hotel_manager
  25 |     await page.fill('#password', 'password123');
  26 |     await page.click('button[type="submit"]');
  27 |     await page.waitForURL('/supplier');
  28 | 
  29 |     // Wait for tabs to be visible
  30 |     await expect(page.locator('text=Hotel Rates')).toBeVisible();
  31 |     
  32 |     // Switch to Hotel Rates tab
  33 |     await page.click('text=Hotel Rates');
  34 | 
  35 |     // CREATE Rate
  36 |     await page.click('button:has-text("Create Rate")');
  37 |     await page.waitForSelector('input[name="hotelName"]');
  38 |     await page.fill('input[name="hotelName"]', rateData.hotelName);
  39 |     await page.fill('input[name="roomType"]', rateData.roomType);
  40 |     await page.fill('input[name="price"]', rateData.price);
  41 |     // validFrom and validTo have defaults, we can leave them
  42 |     await page.click('button:has-text("Save Rate")');
  43 | 
  44 |     // READ / Verify it appears
> 45 |     await expect(page.locator(`text=${rateData.hotelName}`).first()).toBeVisible();
     |                                                                      ^ Error: expect(locator).toBeVisible() failed
  46 | 
  47 |     // The row contains our hotelName. We need to find the edit button in that specific row.
  48 |     const row = page.locator('tr', { hasText: rateData.hotelName }).first();
  49 |     
  50 |     // UPDATE Rate (The edit button is the first ghost button in the row)
  51 |     const editButton = row.locator('button').first();
  52 |     await editButton.click();
  53 |     await page.waitForSelector('input[name="price"]');
  54 |     await page.fill('input[name="price"]', rateData.newPrice);
  55 |     await page.click('button:has-text("Save Rate")');
  56 | 
  57 |     // Verify update
  58 |     await expect(page.locator('tr', { hasText: rateData.hotelName }).locator(`text=${rateData.newPrice}`)).toBeVisible();
  59 | 
  60 |     // DELETE Rate (The delete button is the text-destructive button in the row)
  61 |     // Playwright handles window.confirm automatically by dismissing or accepting. Let's explicitly accept it.
  62 |     page.once('dialog', dialog => dialog.accept());
  63 |     const deleteButton = page.locator('tr', { hasText: rateData.hotelName }).first().locator('button.text-destructive');
  64 |     await deleteButton.click();
  65 | 
  66 |     // Verify deletion
  67 |     await expect(page.locator(`text=${rateData.hotelName}`)).toBeHidden();
  68 |   });
  69 | });
  70 | 
```